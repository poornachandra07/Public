(From d In TopGoldDT.AsEnumerable
Group d By k=d("Sol").toString.Trim Into grp=Group
Let c = grp.Sum(Function (r) CDbl(r("Amount").toString.Trim))
Let ra = New Object(){k,grp.First()("A/c No"),grp.First()("Borrower"),c,grp.First()("Scheme code"),grp.First()("GL code"),grp.Count}
Select SumDT.Rows.Add(ra)).OrderByDescending(Function(m) CDbl(m("Amount"))).Take(10).CopyToDataTable
